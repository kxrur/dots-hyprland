# See https://wiki.hyprland.org/Configuring/Binds/
#!
##! User
bind = Ctrl+Super, Slash, exec, xdg-open ~/.config/illogical-impulse/config.json # Edit shell config
bind = Ctrl+Super+Alt, Slash, exec, xdg-open ~/.config/hypr/custom/keybinds.conf # Edit extra keybinds

# Add stuff here
# Use #! to add an extra column on the cheatsheet
# Use ##! to add a section in that column
# Add a comment after a bind to add a description, like above
#
#
# ------------------------- UNBIND ------------------------- 

# workspace preview redundant
unbind = Super, Super_L
# have to do it twice
unbind = Super+Shift, Super_L
unbind = Super+Shift, Super_L

# lock on l !!
unbind = Super, L
unbind = Super+Shift, L
# on screen keyboard on k !!
unbind = Super, K

# super+alt ???
unbind = Super+Alt, code:10
unbind = Super+Alt, code:11
unbind = Super+Alt, code:12
unbind = Super+Alt, code:13
unbind = Super+Alt, code:14
unbind = Super+Alt, code:15
unbind = Super+Alt, code:16
unbind = Super+Alt, code:17
unbind = Super+Alt, code:18
unbind = Super+Alt, code:19

unbind = Super+Alt, code:87
unbind = Super+Alt, code:88
unbind = Super+Alt, code:89
unbind = Super+Alt, code:83
unbind = Super+Alt, code:84
unbind = Super+Alt, code:85
unbind = Super+Alt, code:79
unbind = Super+Alt, code:80
unbind = Super+Alt, code:81
unbind = Super+Alt, code:90

unbind = Super, code:10
unbind = Super, code:11
unbind = Super, code:12
unbind = Super, code:13
unbind = Super, code:14
unbind = Super, code:15
unbind = Super, code:16
unbind = Super, code:17
unbind = Super, code:18
unbind = Super, code:19

unbind = Super, code:87
unbind = Super, code:88
unbind = Super, code:89
unbind = Super, code:83
unbind = Super, code:84
unbind = Super, code:85
unbind = Super, code:79
unbind = Super, code:80
unbind = Super, code:81
unbind = Super, code:90

# terminal 
unbind = Super, T
unbind = Super, Return


# too many missclick (quit)
unbind = Super, Q

# leave fullscreen
unbind = Super, D

# remove second bind for special toggle
unbind = Super+Control, S

#remove useless ai popup
unbind = Super, O

# remove volume bind - DSNT WORK
# unbind =, XF86AudioRaiseVolume
# unbind =, XF86AudioLowerVolume

#zed?
unbind = Super, Z
#chrome?
unbind = Super, W


# scrollwheel workspace
unbind = Super, mouse_up
unbind = Super, mouse_down

#alt tab
unbind = Super, Tab

unbind = Super+Alt, F

# ------------------------- BIND ------------------------- 

bindl= Super+Control, S, exec, mkdir -p ~/Pictures/Screenshots && ~/.config/ags/scripts/grimblast.sh --freeze copysave area ~/Pictures/Screenshots/Screenshot_"$(date '+%Y-%m-%d_%H.%M.%S')".png # Screenshot area

bind = Super, D, exec, pkill anyrun || anyrun # App launcher

bind = Super+Shift, D, fullscreen, 1 # Exit fullscreen

bind = Super+Shift, Q, killactive, # Quit window

bind = Super, Return, exec, kitty # Terminal

bind = Super+Shift, U, exec, loginctl lock-session # Lock screen
bind = Super+Control+Shift, U, exec, loginctl lock-session # Lock screen (alt)
bindl = Super+Shift, U, exec, sleep 0.1 && systemctl suspend || loginctl suspend # Suspend

# focus
bind = Super, h, movefocus, l # Focus left
bind = Super, l, movefocus, r # Focus right
bind = Super, k, movefocus, u # Focus up
bind = Super, j, movefocus, d # Focus down

# Move window to workspace Super + Shift + [0-9]
bind = Ctrl+Shift, S, movetoworkspace, special # Move to special workspace

bind = Super+Shift, h, movewindow, l # Move window left
bind = Super+Shift, l, movewindow, r # Move window right
bind = Super+Shift, j, movewindow, d # Move window down
bind = Super+Shift, k, movewindow, u # Move window up

bind = Super+Shift, o, togglesplit # Toggle split

#/# bind = Super+Alt, Hash,, # Send to workspace # (1, 2, 3,...)
#bind = Super+Shift, 1, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 1 # [hidden]
#bind = Super+Shift, 2, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 2 # [hidden]
#bind = Super+Shift, 3, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 3 # [hidden]
#bind = Super+Shift, 4, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 4 # [hidden]
#bind = Super+Shift, 5, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 5 # [hidden]
#bind = Super+Shift, 6, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 6 # [hidden]
#bind = Super+Shift, 7, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 7 # [hidden]
#bind = Super+Shift, 8, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 8 # [hidden]
#bind = Super+Shift, 9, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 9 # [hidden]
#bind = Super+Shift, 0, exec, ~/.config/hypr/hyprland/scripts/workspace_action.sh movetoworkspacesilent 10 # [hidden]

# Switch workspaces with mainMod + [0-5]
bind = Super, 1, split-workspace, 1
bind = Super, 2, split-workspace, 2
bind = Super, 3, split-workspace, 3
bind = Super, 4, split-workspace, 4
bind = Super, 5, split-workspace, 5

# Move active window to a workspace with mainMod + SHIFT + [0-5]
bind = Super SHIFT, 1, split-movetoworkspacesilent, 1
bind = Super SHIFT, 2, split-movetoworkspacesilent, 2
bind = Super SHIFT, 3, split-movetoworkspacesilent, 3
bind = Super SHIFT, 4, split-movetoworkspacesilent, 4
bind = Super SHIFT, 5, split-movetoworkspacesilent, 5

bind = Super+Shift, w, exec, zen-browser --p kxr # Browser (personal)
bind = Ctrl+Super+Shift, w, exec, zen-browser --p wok # Browser (work)

bindm = Super, Z, movewindow # Move window

bind = Super, w, exec, zen-browser # Browser

# scrollwheel workspace
bind = Super, mouse_up, workspace, e+1 # [hidden]
bind = Super, mouse_down, workspace, e-1 # [hidden]

# rotate monitor
bind = , code:157, exec, ~/.config/ags/scripts/hyprland/toggle_monitor_transform.sh # Rotate monitor

bind = Super+Alt, F, fullscreenstate, 0 2 # Fullscreen fake

bindd = Super, d, Toggle overview, global, quickshell:overviewWorkspacesToggle # [hidden] Toggle launcher (alt)

# submap (virtual box)
bind = Super, Delete, submap, clean

submap = clean
bind = Super, Home, submap, global

submap = global
